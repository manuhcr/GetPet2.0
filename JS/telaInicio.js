const animation = lottie.loadAnimation({
      container: document.getElementById('lottie-bg'),
      renderer: 'svg',
      loop: true,
      autoplay: true,
      path: 'paws animation.json'  // <-- aqui vai o arquivo JSON de patinhas
    });